# ─────────────────────────────────────────────
# requirements.txt — Skew Analyzer dependencies
# Install: pip install -r requirements.txt
# ─────────────────────────────────────────────

# Core numerical
numpy>=1.24.0
scipy>=1.11.0

# Options data (free tier)
yfinance>=0.2.40

# Anthropic API
anthropic>=0.30.0

# HTTP (for Tradier / Polygon backends)
requests>=2.31.0

# Python scheduler (optional — for --python scheduler mode)
schedule>=1.2.0
